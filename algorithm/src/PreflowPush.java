
/**
 * the PreflowPush class implements the PreoflowPush algorithm
 * 
 * @author Todd Robbins & Brandon Lioce
 */
public class PreflowPush {

    private SimpleGraph sGraph;
//    private HeightList heights;
	
	/**
	 * find the max-flow
	 * @param sGraph graph
	 * @param s source
	 * @param t sink
	 */
	public PreflowPush(SimpleGraph sGraph) {
        this.sGraph = sGraph;
//        heights = new HeightList(graph.numVertices()+1);

//        initialize();
	}
}
